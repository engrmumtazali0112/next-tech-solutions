# ✅ Python 3.14 compatible — all packages use >= so pip picks
#    the latest pre-built wheel for your Python version.
#    No source compilation, no Rust needed.

# ── Web framework ────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ── Database (MySQL) ─────────────────────────────────────────
sqlalchemy>=2.0.36
pymysql>=1.1.1

# ── Pydantic ─────────────────────────────────────────────────
# pydantic v2.9+ ships cp314 wheels — no Rust compile needed
pydantic>=2.9.0
pydantic-settings>=2.5.0

# ── File uploads & images ────────────────────────────────────
python-multipart>=0.0.12
Pillow>=11.0.0          # 11.x has cp314 wheels
aiofiles>=23.2.1

# ── Email ────────────────────────────────────────────────────
email-validator>=2.2.0

# ── Utils ────────────────────────────────────────────────────
python-dotenv>=1.0.1
python-slugify>=8.0.4
httpx>=0.27.0